<%= form_for(@word) do |f| %>
  <% if @word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% @word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :namelanguage1 %><br>
    <%= f.text_field :namelanguage1 %>
  </div>
  <div class="field">
    <%= f.label :namelanguage2 %><br>
    <%= f.text_field :namelanguage2 %>
  </div>
  <div class="field">
    <%= f.label :language1_id %><br>
     <%= f.collection_select :language1_id, @all_languages, :id, :name, {}, {:class=>'form-control input'} %>
  </div>
  <div class="field">
    <%= f.label :language2_id %><br>
     <%= f.collection_select :language2_id, @all_languages, :id, :name, {}, {:class=>'form-control input'} %>
  </div>
  
    <div class="field">
    <%= f.label :tags %><br />
    <%= f.collection_select :tag_ids, @all_tags, :id, :name,  {}, { :multiple => true, :class=>'form-control'} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>